[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "my_project"
version = "0.1.0"
description = "Basic Python package structure"
readme = "README.md"
requires-python = ">=3.10"
